# خطة الترقية والتطوير المقترحة (الترقية إلى الإصدار V5.2 - نسخة محدثة)

مرحباً بك! بناءً على توجيهاتك القيمة، قمنا بتعديل الخطة البرمجية لتكون **ديناميكية بالكامل**. لن نعتمد على قوائم مقاسات ثابتة أو محددة مسبقاً، بل سيقوم النظام تلقائياً بقراءة المقاسات المدخلة فعلياً في بيانات موظفيك وعرض إحصاءاتها بشكل مباشر وتلقائي.

---

## User Review Required

> [!IMPORTANT]
> **1. إحصاء ديناميكي مرن (Dynamic Size Statistics):**
> * سيتفحص النظام تلقائياً أعمدة القياسات للموظفين الموجودين في القائمة حالياً.
> * سيتم تجميع كافة القياسات الفريدة المدخلة فعلياً (سواء كانت مقاسات أحذية مثل 40, 42, 45 أو مقاسات بدلات مثل XL, Medium, Large) وحساب تكرار كل مقاس ونسبته المئوية.
> * سيتم فرز المقاسات بشكل تلقائي وذكي (ترتيب رقمي للأحذية، وترتيب منطقي أو أبجدي للبدلات) وعرضها في مخططات أشرطة تقدم أفريقية.
> * **الميزة:** هذا التعديل يجعل النظام متوافقاً مع أي نمط إدخال (حروف أو أرقام) دون الحاجة لأي برمجة مستقبلية في حال إدخال مقاسات جديدة.

> [!TIP]
> **2. تبسيط الاستمارة (إلغاء نوع العمل والحالة):**
> * إلغاء سطر **"نوع العمل والحالة"** من بطاقة معاينة وطباعة الموظف الفردية A4 لتبسيط الاستمارة التعريفية للموظف.

---

## Proposed Changes

### [Core Features]

#### [MODIFY] [نظام_ادراة_الملاك_v5.html](file:///E:/Antigravity%20projects/HR%20Admin/%D9%86%D8%B8%D8%A7%D9%85_%D8%A7%D8%AF%D8%B1%D8%A7%D8%A9_%D8%A7%D9%84%D9%85%D9%84%D8%A7%D9%83_v5.html)

1. **إضافة متغير الحالة (React State):**
   * إدراج `const [showSizeDetails, setShowSizeDetails] = useState(false);` للتحكم بظهور لوحة الإحصاءات الديناميكية.
2. **تعديل الاستمارة المطبوعة A4:**
   * حذف سطر `نوع العمل والحالة` (الأسطر 3643-3649) من الجدول التعريفي المطبوع لتبسيط الاستمارة.
3. **تطوير لوحة الإحصاء الديناميكية للقياسات:**
   * قراءة وتجميع مقاسات البدلات والأحذية المدخلة فعلياً وتصنيفها وحساب تكرارها.
   * ترتيب مقاسات البدلات منطقياً (أبجدياً/أحجام) ومقاسات الأحذية عددياً.
   * رسم أشرطة تقدم بصرية تفاعلية لكل مقاس موجود في البيانات.

---

## Verification Plan

### الفحص والتحقق:
1. **اختبار الديناميكية:** إدخال مقاس غير معتاد (مثال: مقاس حذاء 47 أو قياس بدلة 5XL) والتحقق من ظهور المقاس الجديد فوراً في لوحة الإحصاء ونسبته المئوية دون مشاكل.
2. **اختبار الاستمارة:** فتح طباعة الاستمارة للموظف والتحقق من إلغاء سطر "نوع العمل والحالة".
3. **المزامنة:** مطابقة الكود بدقة بين مجلد العمل وسطح المكتب.
